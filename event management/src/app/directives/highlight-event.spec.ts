import { HighlightEvent } from './highlight-event';

describe('HighlightEvent', () => {
  it('should create an instance', () => {
    const directive = new HighlightEvent();
    expect(directive).toBeTruthy();
  });
});
